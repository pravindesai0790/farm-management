import { ChangeDetectionStrategy, Component, DestroyRef, OnInit, inject, signal } from '@angular/core'; import { takeUntilDestroyed } from '@angular/core/rxjs-interop'; import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatInputModule } from '@angular/material/input'; import { MatProgressSpinnerModule } from '@angular/material/progress-spinner'; import { MatSelectModule } from '@angular/material/select'; import { MatSnackBar } from '@angular/material/snack-bar'; import { ActivatedRoute,Router,RouterLink } from '@angular/router'; import { finalize,of } from 'rxjs'; import { FarmManagementService } from '../../core/farm-management/farm-management.service'; import { getApiErrorMessage } from '../../core/models/api-error.model';
@Component({selector:'app-crop-editor-page',standalone:true,imports:[MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule,MatProgressSpinnerModule,MatSelectModule,ReactiveFormsModule,RouterLink],changeDetection:ChangeDetectionStrategy.OnPush,template:`<div class="section-heading"><a mat-button routerLink="/crops">← Back to crops</a><p class="eyebrow">Crop catalogue</p><h2>{{id?'Edit crop':'New crop'}}</h2></div>@if(isLoading()){<div class="loading-state"><mat-spinner diameter="36"/></div>}@else{<mat-card><mat-card-content><form [formGroup]="form" (ngSubmit)="submit()"><div class="form-grid"><mat-form-field appearance="outline"><mat-label>Code</mat-label><input matInput formControlName="code"/>@if(form.controls.code.hasError('required')){<mat-error>Code is required.</mat-error>}</mat-form-field><mat-form-field appearance="outline"><mat-label>Name</mat-label><input matInput formControlName="name"/>@if(form.controls.name.hasError('required')){<mat-error>Name is required.</mat-error>}</mat-form-field><mat-form-field appearance="outline"><mat-label>Scientific name</mat-label><input matInput formControlName="scientificName"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Crop type</mat-label><input matInput formControlName="cropType" placeholder="GENERAL"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Duration</mat-label><mat-select formControlName="cropDurationType"><mat-option value="ANNUAL">Annual</mat-option><mat-option value="PERENNIAL">Perennial</mat-option><mat-option value="SEASONAL">Seasonal</mat-option><mat-option value="OTHER">Other</mat-option></mat-select></mat-form-field><mat-form-field appearance="outline" class="wide"><mat-label>Description</mat-label><textarea matInput rows="3" formControlName="description"></textarea></mat-form-field></div>@if(errorMessage()){<p class="form-error">{{errorMessage()}}</p>}<div class="form-actions"><a mat-button routerLink="/crops">Cancel</a><button mat-flat-button color="primary" [disabled]="isSubmitting()">{{id?'Save changes':'Create crop'}}</button></div></form></mat-card-content></mat-card>}`,styles:[`:host{display:block}.section-heading{margin-bottom:20px}.section-heading h2{margin:4px 0}.form-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:16px}.wide{grid-column:1/-1}.form-actions{display:flex;justify-content:flex-end;gap:12px;margin-top:20px}.form-error{color:#b3261e}@media(max-width:640px){.form-grid{grid-template-columns:1fr}.wide{grid-column:auto}}`]})
export class CropEditorPageComponent implements OnInit{private readonly service=inject(FarmManagementService);private readonly route=inject(ActivatedRoute);private readonly router=inject(Router);private readonly snack=inject(MatSnackBar);private readonly destroyRef=inject(DestroyRef);private readonly fb=inject(FormBuilder);readonly id=this.route.snapshot.paramMap.get('id');readonly isLoading=signal(true);readonly isSubmitting=signal(false);readonly errorMessage=signal<string|null>(null);readonly form=this.fb.nonNullable.group({code:['',[Validators.required]],name:['',[Validators.required]],scientificName:[''],cropType:['GENERAL',[Validators.required]],cropDurationType:['ANNUAL',[Validators.required]],description:['']});ngOnInit():void{const request$=this.id?this.service.getCrop(this.id):of(null as any);request$.pipe(takeUntilDestroyed(this.destroyRef),finalize(()=>this.isLoading.set(false))).subscribe({next:r=>{if(r)this.form.patchValue({code:r.code,name:r.name,scientificName:r.scientificName??'',cropType:r.cropType,cropDurationType:r.cropDurationType,description:r.description??''});},error:e=>this.errorMessage.set(getApiErrorMessage(e,'Crop could not be loaded.'))});}submit():void{if(this.form.invalid){this.form.markAllAsTouched();return;}this.isSubmitting.set(true);const request=this.id?this.service.updateCrop(this.id,this.form.getRawValue()):this.service.createCrop(this.form.getRawValue());request.pipe(takeUntilDestroyed(this.destroyRef),finalize(()=>this.isSubmitting.set(false))).subscribe({next:()=>{this.snack.open(this.id?'Crop updated.':'Crop created.','Dismiss',{duration:3000});void this.router.navigateByUrl('/crops');},error:e=>this.errorMessage.set(getApiErrorMessage(e,'Crop could not be saved.'))});}}
