import { ChangeDetectionStrategy, Component, DestroyRef, OnInit, inject, signal } from '@angular/core'; import { takeUntilDestroyed } from '@angular/core/rxjs-interop'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatInputModule } from '@angular/material/input'; import { MatProgressSpinnerModule } from '@angular/material/progress-spinner'; import { MatSelectModule } from '@angular/material/select'; import { MatSnackBar } from '@angular/material/snack-bar'; import { ActivatedRoute, Router, RouterLink } from '@angular/router'; import { forkJoin, of, finalize } from 'rxjs'; import { FarmManagementService } from '../../core/farm-management/farm-management.service'; import { getApiErrorMessage } from '../../core/models/api-error.model';
@Component({selector:'app-plantation-editor-page',standalone:true,imports:[MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule,MatProgressSpinnerModule,MatSelectModule,ReactiveFormsModule,RouterLink],changeDetection:ChangeDetectionStrategy.OnPush,template:`<div class="section-heading"><a mat-button routerLink="/plantations">← Back to plantations</a><p class="eyebrow">Growing operation</p><h2>{{id?'Edit plantation':'New plantation'}}</h2></div>@if(isLoading()){<div class="loading-state"><mat-spinner diameter="36"/></div>}@else{<mat-card><mat-card-content><form [formGroup]="form" (ngSubmit)="submit()"><div class="form-grid"><mat-form-field appearance="outline"><mat-label>Farm</mat-label><mat-select formControlName="farmId" (valueChange)="loadAreas($event)">@for(farm of farms();track farm.id){<mat-option [value]="farm.id">{{farm.name}} ({{farm.code}})</mat-option>}</mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>Farm area</mat-label><mat-select formControlName="farmAreaId">@for(area of areas();track area.id){<mat-option [value]="area.id">{{area.name}} ({{area.totalArea}} {{area.areaUnitSymbol}})</mat-option>}</mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>Crop</mat-label><mat-select formControlName="cropId" (valueChange)="loadVarieties($event)">@for(crop of crops();track crop.id){<mat-option [value]="crop.id">{{crop.name}} ({{crop.code}})</mat-option>}</mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>Variety</mat-label><mat-select formControlName="varietyId"><mat-option [value]="null">No variety</mat-option>@for(v of varieties();track v.id){<mat-option [value]="v.id">{{v.name}}</mat-option>}</mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>Plantation code</mat-label><input matInput formControlName="plantationCode"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Plantation name</mat-label><input matInput formControlName="plantationName"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Allocated area</mat-label><input matInput type="number" min="0.01" formControlName="allocatedArea"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Area unit</mat-label><mat-select formControlName="areaUnitId">@for(unit of units();track unit.id){<mat-option [value]="unit.id">{{unit.name}} ({{unit.symbol}})</mat-option>}</mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>Planting date</mat-label><input matInput type="date" formControlName="plantingDate"/></mat-form-field><mat-form-field appearance="outline"><mat-label>Expected end date</mat-label><input matInput type="date" formControlName="expectedEndDate"/></mat-form-field></div>@if(errorMessage()){<p class="form-error">{{errorMessage()}}</p>}<div class="form-actions"><a mat-button routerLink="/plantations">Cancel</a><button mat-flat-button color="primary" [disabled]="isSubmitting()">{{id?'Save changes':'Create plantation'}}</button></div></form></mat-card-content></mat-card>}`,styles:[`:host{display:block}.section-heading{margin-bottom:20px}.section-heading h2{margin:4px 0}.form-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:16px}.form-actions{display:flex;justify-content:flex-end;gap:12px;margin-top:20px}.form-error{color:#b3261e}@media(max-width:640px){.form-grid{grid-template-columns:1fr}}`]})
export class PlantationEditorPageComponent implements OnInit{private readonly service=inject(FarmManagementService);private readonly route=inject(ActivatedRoute);private readonly router=inject(Router);private readonly snack=inject(MatSnackBar);private readonly destroyRef=inject(DestroyRef);private readonly fb=inject(FormBuilder);readonly id=this.route.snapshot.paramMap.get('id');readonly isLoading=signal(true);readonly isSubmitting=signal(false);readonly errorMessage=signal<string|null>(null);readonly farms=signal<readonly any[]>([]);readonly areas=signal<readonly any[]>([]);readonly crops=signal<readonly any[]>([]);readonly varieties=signal<readonly any[]>([]);readonly units=signal<readonly any[]>([]);readonly form=this.fb.group({farmId:[null as string|null,[Validators.required]],farmAreaId:[null as string|null,[Validators.required]],cropId:[null as string|null,[Validators.required]],varietyId:[null as string|null],plantationCode:['',[Validators.required]],plantationName:['',[Validators.required]],allocatedArea:[null as number|null,[Validators.required,Validators.min(.01)]],areaUnitId:[null as string|null,[Validators.required]],plantingDate:['',[Validators.required]],expectedEndDate:['']});ngOnInit():void{forkJoin({farms:this.service.listFarms(1,100,'',true),crops:this.service.listCrops(1,100,'',true),units:this.service.listUnits(),plantation:this.id?this.service.getPlantation(this.id):of(null)}).pipe(takeUntilDestroyed(this.destroyRef),finalize(()=>this.isLoading.set(false))).subscribe({next:r=>{this.farms.set(r.farms.items);this.crops.set(r.crops.items);this.units.set(r.units);if(r.plantation){this.form.patchValue({farmId:r.plantation.farmId,farmAreaId:r.plantation.farmAreaId,cropId:r.plantation.cropId,varietyId:r.plantation.varietyId,plantationCode:r.plantation.plantationCode,plantationName:r.plantation.plantationName,allocatedArea:r.plantation.allocatedArea,areaUnitId:r.plantation.areaUnitId,plantingDate:r.plantation.plantingDate,expectedEndDate:r.plantation.expectedEndDate??''});this.loadAreas(r.plantation.farmId);this.loadVarieties(r.plantation.cropId);}else if(r.farms.items[0]){this.form.controls.farmId.setValue(r.farms.items[0].id);this.loadAreas(r.farms.items[0].id);}},error:e=>this.errorMessage.set(getApiErrorMessage(e,'Plantation form data could not be loaded.'))});}loadAreas(farmId:string):void{this.form.controls.farmAreaId.setValue(null);this.service.listAreas(farmId,true).pipe(takeUntilDestroyed(this.destroyRef)).subscribe(r=>this.areas.set(r));}loadVarieties(cropId:string):void{this.form.controls.varietyId.setValue(null);this.service.listVarieties(cropId).pipe(takeUntilDestroyed(this.destroyRef)).subscribe(r=>this.varieties.set(r.items));}submit():void{if(this.form.invalid){this.form.markAllAsTouched();return;}this.isSubmitting.set(true);const value=this.form.getRawValue();const request=this.id?this.service.updatePlantation(this.id,value):this.service.createPlantation(value);request.pipe(takeUntilDestroyed(this.destroyRef),finalize(()=>this.isSubmitting.set(false))).subscribe({next:()=>{this.snack.open(this.id?'Plantation updated.':'Plantation created.','Dismiss',{duration:3000});void this.router.navigateByUrl('/plantations');},error:e=>this.errorMessage.set(getApiErrorMessage(e,'Plantation could not be saved.'))});}}
